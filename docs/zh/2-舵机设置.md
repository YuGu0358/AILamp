# 2. 舵机设置

固定 ID：

```text
base_yaw=1
base_pitch=2
elbow_pitch=3
wrist_roll=4
wrist_pitch=5
```

测试：

```bash
ailamp motor-test
```

校准沿用 LeLamp runtime：

```bash
uv run -m lelamp.setup_motors --id ailamp --port /dev/ttyACM0
sudo uv run -m lelamp.calibrate --id ailamp --port /dev/ttyACM0
```
