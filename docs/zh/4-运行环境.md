# 4. 运行环境

```bash
cd ~/projects/AILamp
python3 -m venv .venv
source .venv/bin/activate
pip install -e ".[hardware,simulation,voice,test]"
```

检测：

```bash
ailamp hardware-check
ailamp hardware-check --include-devices
```

Pico WH 固件在：

```text
firmware/pico_led_controller/code.py
```

